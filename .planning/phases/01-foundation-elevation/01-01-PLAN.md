---
phase: 01-foundation-elevation
plan: 01
type: execute
---

<objective>
Establish Haskell project structure with Cabal build system and type-safe GADT architecture for world generation pipeline.

Purpose: Create the foundational scaffolding and type system that all subsequent phases will build upon, ensuring impossible world states are unrepresentable at compile time.
Output: Working Cabal project with GADT World types, module structure, and all dependencies configured.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-elevation/1-RESEARCH.md

**Tech stack from research:**
- GHC 9.14+ (LTS release)
- Cabal 3.16+ (standard build system)
- massiv (multi-dimensional arrays, 3x faster than repa)
- pure-noise 0.2.1+ (coherent noise, 84-95% C++ performance)
- vector, deepseq (supporting libraries)

**Established patterns from research:**
- Vertical module organization (avoid separate Types modules)
- GADT for type-safe world state progression
- Strict fields to prevent space leaks
- Cabal over Stack (current standard)

**Key decisions from research:**
- Use Cabal + ghcup (not Stack) - Cabal is now the standard
- Use massiv for arrays (not hmatrix/repa) - Avoids BLAS dependency hell, better performance
- Use pure-noise (not hsnoise) - More noise types, better performance
- GADTs with 3-5 phases max (avoid complexity explosion)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Cabal project with dependencies</name>
  <files>axiom.cabal, cabal.project, .gitignore, src/Main.hs</files>
  <action>
    Create Cabal project structure:
    1. Run `cabal init --non-interactive --minimal` to bootstrap
    2. Edit axiom.cabal to add dependencies: base >=4.18, massiv, pure-noise, vector, deepseq
    3. Set default-language: GHC2021 (modern Haskell)
    4. Add ghc-options: -Wall -O2 (warnings + optimization)
    5. Create cabal.project file (empty for now, enables multi-package later)
    6. Add .gitignore with: dist-newstyle/, *.hi, *.o, .ghc.environment.*
    7. Create placeholder src/Main.hs with module Main and main = putStrLn "Axiom initialized"

    DO NOT use Stack (Cabal is the current standard per research).
    DO NOT add hmatrix dependency (causes BLAS/LAPACK issues, massiv is sufficient).
    DO NOT use old Haskell2010 language (use GHC2021 for modern features).
  </action>
  <verify>cabal build succeeds without errors, cabal run axiom prints "Axiom initialized"</verify>
  <done>Project builds cleanly, all dependencies resolve, basic executable runs</done>
</task>

<task type="auto">
  <name>Task 2: Create GADT World types and module structure</name>
  <files>src/Axiom/World.hs, src/Axiom/Geo/Elevation.hs, src/Axiom/Geo/Noise.hs</files>
  <action>
    Create module structure following vertical organization pattern:

    1. src/Axiom/World.hs:
       - Enable extensions: {-# LANGUAGE GADTs, DataKinds, KindSignatures #-}
       - Define Phase kind: data Phase = Empty | HasElevation | Complete
       - Define GADT: data World (p :: Phase) where
         - EmptyWorld :: World 'Empty
         - WithElevation :: ElevationMap -> World 'Empty -> World 'HasElevation
         (Complete constructor will be added in later phases)
       - Export: World(..), Phase(..)

    2. src/Axiom/Geo/Elevation.hs:
       - Import massiv: Data.Massiv.Array as A
       - Define ElevationMap data type with strict fields:
         data ElevationMap = ElevationMap
           { emGrid :: !(Array U Ix2 Double)
           , emSeed :: !Int
           }
       - Stub functions: generate :: Int -> ElevationMap (placeholder returning dummy array)
       - Export: ElevationMap(..), generate

    3. src/Axiom/Geo/Noise.hs:
       - Import qualified Noise
       - Stub function: terrainNoise :: Double -> Double -> Double (placeholder returning 0.0)
       - Export: terrainNoise

    4. Update src/Main.hs:
       - Import Axiom.World (World(..), Phase(..))
       - Create test: let w = EmptyWorld :: World 'Empty
       - Print: "World initialized: Empty phase"

    Use STRICT fields (! prefix) to prevent space leaks per research.
    DO NOT create separate src/Axiom/Types.hs (causes coupling, violates vertical organization).
    DO NOT add more than 3-5 GADT phases (complexity explosion per research).
  </action>
  <verify>cabal build succeeds, cabal run axiom prints "World initialized: Empty phase", no warnings</verify>
  <done>GADT World types compile, module structure follows vertical pattern, strict fields prevent space leaks, basic type safety demonstrated</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cabal build completes without errors or warnings
- [ ] cabal run axiom executes and prints expected output
- [ ] All dependencies (massiv, pure-noise, vector, deepseq) are in axiom.cabal
- [ ] GADT World type compiles with Phase kind and constructors
- [ ] Module structure follows vertical organization (no separate Types module)
</verification>

<success_criteria>

- Cabal project builds cleanly
- All dependencies configured correctly
- GADT World types demonstrate compile-time type safety
- Module structure follows research patterns (vertical organization, strict fields)
- No warnings or errors
- Basic executable runs
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-elevation/01-01-SUMMARY.md`:

# Phase 1 Plan 1: Foundation Setup Summary

**Haskell project scaffolded with type-safe GADT architecture and modern Cabal build system**

## Accomplishments

- Initialized Cabal project with GHC2021 and optimized build flags
- Configured dependencies: massiv, pure-noise, vector, deepseq
- Implemented GADT World types with Phase kind (Empty, HasElevation)
- Created vertical module structure (Axiom.World, Axiom.Geo.Elevation, Axiom.Geo.Noise)
- Demonstrated compile-time type safety with EmptyWorld constructor

## Files Created/Modified

- `axiom.cabal` - Package definition with dependencies and build configuration
- `cabal.project` - Multi-package support (empty for now)
- `.gitignore` - Haskell build artifacts
- `src/Main.hs` - Executable entry point with World initialization test
- `src/Axiom/World.hs` - GADT World types and Phase kind
- `src/Axiom/Geo/Elevation.hs` - ElevationMap type with strict fields
- `src/Axiom/Geo/Noise.hs` - Noise function stubs

## Decisions Made

- Cabal over Stack (current standard per research)
- massiv over hmatrix (avoids BLAS dependency issues)
- GHC2021 language standard (modern Haskell features)
- Vertical module organization (no separate Types module)
- Strict fields in data types (prevent space leaks)

## Issues Encountered

None

## Next Step

Ready for 01-02-PLAN.md (Noise & Elevation implementation)
</output>
